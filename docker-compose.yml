version: '3.7'

services:

  nginx:
    image: nginx:latest
    container_name: nginx
    ports: # Exposed to host and other services
      - "80:80"  # 8080:80 Map port 80 in the container to port 8080 on the Docker host.
    depends_on: #Wait for uwsgi to be ready
      - 
    restart: always

  web: # Flask and uWSGI
    container_name: web 
    build: ./web
    expose:
      - "80"
    

  # db:
  #   image: postgres:latest
  #   expose: # Exposed only to services
  #     - "5432:5432"
  #   restart: always
  # 
  # 

networks:

${COMPOSE_PROJECT_NAME}
      